import {bootstrap}    from 'angular2/platform/browser' //Angular's browser bootstrap function
import {AppComponent} from './app.component' //The component we created earlier

//Then we call bootstrap, passing in the root component type, AppComponent.
bootstrap(AppComponent);